{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "headers": [
    {
      "//": "HTML and live data must be re-checked every time. The JS and CSS they point at carry a content hash in the query string (tools/stamp-assets.mjs), so a changed file is a changed URL and no cache can answer for it with the old one.",
      "source": "/(.*)\\.(html|json)",
      "headers": [{ "key": "Cache-Control", "value": "public, max-age=0, must-revalidate" }]
    },
    {
      "//": "Directory URLs resolve to index.html and do not match the pattern above.",
      "source": "/",
      "headers": [{ "key": "Cache-Control", "value": "public, max-age=0, must-revalidate" }]
    },
    {
      "source": "/((?!.*\\.).*)",
      "headers": [{ "key": "Cache-Control", "value": "public, max-age=0, must-revalidate" }]
    }
  ]
}
